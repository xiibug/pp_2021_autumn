#ifndef KRUGLOV_TASK1_VECTOR_MAX_ELEMENT
#define KRUGLOV_TASK1_VECTOR_MAX_ELEMENT

#include <vector>

std::vector<int> get_random_vector(std::size_t size, int max_possible);

int get_max_element(std::vector<int>& vec, std::size_t size);

#endif