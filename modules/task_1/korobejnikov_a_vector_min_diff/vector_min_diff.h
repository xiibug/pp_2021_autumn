#ifndef MODULES_TASK_1_KOROBEJNIKOV_A_VECTOR_MIN_DIFF_VECTOR_MIN_DIFF_H_
#define MODULES_TASK_1_KOROBEJNIKOV_A_VECTOR_MIN_DIFF_VECTOR_MIN_DIFF_H_

#include <vector>

std::pair<int, int> sequentialCount(std::vector<int> vec, int len);

std::pair<int, int> parallelCount(std::vector<int> vec, int len);

std::vector<int> getRandomVector(int sz);

#endif
